# 测试配置文件 - 使用更大的打印床以支持更大尺寸的抽屉测试
printer:
  model: h2d  # 使用 H2D 预设 (300x320x325)

opengrid:
  tile_type: Full
  stacking_method: Ironing
  interface_separation: 0.2
  tile_size: 28

output:
  stl_dir: ~/3D打印/opengrid/
