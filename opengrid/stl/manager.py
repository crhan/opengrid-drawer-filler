"""STL file management"""
import os
from pathlib import Path


def generate_and_link_stls(scheme, project_path, copies=1, verbose=False, force=False):
    """Generate STL and link to project"""
    # Placeholder - would call generator.py
    return []
